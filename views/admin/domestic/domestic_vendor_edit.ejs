<!-- ✅ File: /views/admin/domestic/domestic_vendor_edit.ejs -->

<style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #f5f7fa;
      color: #333;
      display: flex;
      flex-direction: column;
      height: 100vh;
    }
  
    .top-section {
      flex: 0 0 20%;
      padding: 0;
      background-color: #f5f7fa;
      overflow-y: auto;
      font-size: 12px;
    }
  
    h2 {
      margin-bottom: 1rem;
      font-size: 20px;
      color: #333;
    }
  
    .form {
      width: 100%;
    }
  
    .form-row {
      display: flex;
      width: 100%;
      gap: 20px;
      margin-bottom: 15px;
    }
  
    .form input,
    .form textarea {
      padding: 3px;
      font-size: 11px;
      box-sizing: border-box;
      height: 24px;
      border-radius: 3px;
      border: 1px solid #ccc;
    }
  
    .form-row.second {
      display: flex;
      width: 100%;
      align-items: center;
      gap: 0.5rem;
    }
  
    button {
      padding: 3px 16px;
      font-size: 11px;
      cursor: pointer;
      border: 1px solid #066da5;
      border-radius: 4px;
      display: inline-block;
      text-align: center;
      vertical-align: middle;
      background-color: #f5f7fa;
    }
  </style>
  
  <body>
    <div class="top-section compact-form">
      <h2>Vendor 수정</h2>
      <form method="POST" action="/admin/domestic/edit/<%= vendor.id %>" class="form" style="width: 100%;">
  
        <!-- ✅ 입력폼: Deposit Rate 제거된 구조 -->
        <div class="form-row first">
          <input type="date" name="date" value="<%= vendor.date.toISOString().split('T')[0] %>" required style="flex: 1 1 5%;">
          <input type="text" name="v_name" value="<%= vendor.v_name %>" required placeholder="Vendor Name" style="flex: 1 1 17%;">
          <input type="text" name="v_address1" value="<%= vendor.v_address1 %>" placeholder="Address" style="flex: 1 1 15%;">
          <input type="text" name="v_address2" value="<%= vendor.v_address2 %>" placeholder="city, state, zip" style="flex: 1 1 15%;">
          <input type="text" name="v_phone" value="<%= vendor.v_phone %>" placeholder="Phone" style="flex: 1 1 5.5%;">
          <input type="email" name="v_email" value="<%= vendor.v_email %>" placeholder="Email" style="flex: 1 1 12%;">
          <textarea name="v_note" placeholder="Note" rows="2" style="flex: 1 1 30%; resize: vertical;"><%= vendor.v_note %></textarea>
        </div>
  
        <!-- ✅ 저장 버튼 / 돌아가기 버튼 -->
        <div class="form-row second" style="display: flex; justify-content: flex-end; align-items: center;">
          <a href="/admin/domestic"><button type="button">🔙 돌아가기</button></a>
          <button type="submit">✏️ 저장</button>
        </div>
  
      </form>
    </div>
  </body>